---
import Advantage from './Advantage.astro'
import list from '../data/advantagelist.json';
// For chevron lists: list-image-[url(/public/chevron_right.svg)]
---

<ul class="list-disc mx-8">

  {list.map((adv) => (
    <Advantage title={adv.lead}>
      {adv.desc}
    </Advantage>
  ))}

</ul>
